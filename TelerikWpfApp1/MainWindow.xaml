<Window x:Class="TelerikWpfApp1.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
                Title="MainWindow" Height="1000" Width="1000" WindowState="Maximized"
        
        x:Name="_thjis">



    
    
    
    <Grid>
        <telerik:RadMap x:Name="mmap" ZoomLevel="4" Center="0,0" MouseSelectionMode="RaiseEvent">
            <telerik:RadMap.Provider>
                <telerik:OpenStreetMapProvider />
            </telerik:RadMap.Provider>
            
            <telerik:VisualizationLayer 
                   x:Name="VisLayer" ItemsSource="{Binding tMapPath2, ElementName=_thjis}" SelectionChanged="VisLayer_SelectionChanged"/>

            <telerik:InformationLayer 
               x:Name="InfLayer" ItemsSource="{Binding tMapPath, ElementName=_thjis}" />

        </telerik:RadMap>

    </Grid>
</Window>
